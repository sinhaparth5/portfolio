<div id="consent-banner" class="banner" data-sketch-card>
	<p>
		Google Analytics tells me what gets read. Essential cookies keep the notebook working even if
		you say no.
	</p>
	<div class="row">
		<button type="button" data-sketch-button="outline" id="consent-essential">essential only</button>
		<button type="button" data-sketch-button="solid" id="consent-all">accept all</button>
	</div>
</div>
<script src="../scripts/consent.ts"></script>

<style>
	.banner {
		position: fixed;
		left: var(--space-2);
		right: var(--space-2);
		bottom: var(--space-2);
		max-width: 30rem;
		margin: 0 auto;
		padding: var(--space-2);
		background: #fff6a9;
		z-index: 100;
		opacity: 0;
		pointer-events: none;
		transform: translateY(12px) rotate(-0.6deg);
		box-shadow: 0.55rem 0.7rem 0 rgb(66 52 37 / 12%);
		transition: opacity 150ms ease, transform 150ms ease;
	}

	.banner.show {
		opacity: 1;
		pointer-events: auto;
		transform: translateY(0) rotate(-0.6deg);
	}

	.banner p {
		margin: 0 0 var(--space-1);
		font-size: 1rem;
		color: var(--ink-soft);
	}

	.row {
		display: flex;
		gap: var(--space-1);
		flex-wrap: wrap;
	}

	@media (max-width: 640px) {
		.banner {
			left: 0.6rem;
			right: 0.6rem;
			bottom: 0.6rem;
		}
	}
</style>
