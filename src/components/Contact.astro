---
const { email, linkedin, twitter } = Astro.props;
---

<section id="contact" class="py-20 md:py-32 bg-[var(--background)] px-6 md:px-12">
  <div class="max-w-6xl mx-auto">
    <div class="text-center mb-16 scroll-reveal">
      <p class="text-sm text-[var(--muted-foreground)] tracking-widest uppercase mb-4">Get in Touch</p>
      <h2 class="text-4xl md:text-[52px] font-[var(--font-heading)] text-[var(--foreground)] leading-tight">
        Let's Work Together
      </h2>
      <div class="w-16 h-[2px] bg-[var(--foreground)] mx-auto mt-4"></div>
    </div>

    <div class="grid md:grid-cols-2 gap-12">
      <div class="space-y-8 scroll-reveal">
        <div class="space-y-6">
          <p class="text-[var(--muted-foreground)] text-lg leading-relaxed">
            I'm always interested in hearing about new projects and opportunities.
            Whether you have a question or just want to say hi, feel free to reach out!
          </p>

          <div class="space-y-4">
            <a
              href={`mailto:${email}`}
              class="flex items-center gap-4 text-[var(--foreground)] hover:text-[var(--accent)] transition-colors group"
            >
              <div class="w-12 h-12 border-2 border-[var(--foreground)] flex items-center justify-center group-hover:bg-[var(--foreground)] group-hover:border-[var(--foreground)] transition-all">
                <svg class="w-5 h-5 group-hover:text-[var(--background)]" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
                </svg>
              </div>
              <div>
                <p class="text-sm text-[var(--muted-foreground)]">Email</p>
                <p class="text-base font-medium break-all">{email}</p>
              </div>
            </a>

            <a
              href={linkedin}
              target="_blank"
              rel="noopener noreferrer"
              class="flex items-center gap-4 text-[var(--foreground)] hover:text-[var(--accent)] transition-colors group"
            >
              <div class="w-12 h-12 border-2 border-[var(--foreground)] flex items-center justify-center group-hover:bg-[var(--foreground)] group-hover:border-[var(--foreground)] transition-all">
                <svg class="w-5 h-5 group-hover:text-[var(--background)]" fill="currentColor" viewBox="0 0 24 24">
                  <path d="M19 0h-14c-2.761 0-5 2.239-5 5v14c0 2.761 2.239 5 5 5h14c2.762 0 5-2.239 5-5v-14c0-2.761-2.238-5-5-5zm-11 19h-3v-11h3v11zm-1.5-12.268c-.966 0-1.75-.79-1.75-1.764s.784-1.764 1.75-1.764 1.75.79 1.75 1.764-.783 1.764-1.75 1.764zm13.5 12.268h-3v-5.604c0-3.368-4-3.113-4 0v5.604h-3v-11h3v1.765c1.396-2.586 7-2.777 7 2.476v6.759z"/>
                </svg>
              </div>
              <div>
                <p class="text-sm text-[var(--muted-foreground)]">LinkedIn</p>
                <p class="text-base font-medium">Connect with me</p>
              </div>
            </a>

            <a
              href={`https://twitter.com/${twitter}`}
              target="_blank"
              rel="noopener noreferrer"
              class="flex items-center gap-4 text-[var(--foreground)] hover:text-[var(--accent)] transition-colors group"
            >
              <div class="w-12 h-12 border-2 border-[var(--foreground)] flex items-center justify-center group-hover:bg-[var(--foreground)] group-hover:border-[var(--foreground)] transition-all">
                <svg class="w-5 h-5 group-hover:text-[var(--background)]" fill="currentColor" viewBox="0 0 24 24">
                  <path d="M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-5.214-6.817L4.99 21.75H1.68l7.73-8.835L1.254 2.25H8.08l4.713 6.231zm-1.161 17.52h1.833L7.084 4.126H5.117z"/>
                </svg>
              </div>
              <div>
                <p class="text-sm text-[var(--muted-foreground)]">Twitter</p>
                <p class="text-base font-medium">@{twitter}</p>
              </div>
            </a>
          </div>
        </div>
      </div>

      <div class="bg-[var(--secondary-background)] p-8 scroll-reveal">
        <form class="space-y-6" method="POST" action="https://formspree.io/f/YOUR_FORM_ID">
          <div>
            <label for="name" class="block text-sm text-[var(--secondary-foreground)] mb-2">
              Name
            </label>
            <input
              type="text"
              id="name"
              name="name"
              required
              class="w-full px-4 py-3 bg-[var(--background)] border-2 border-[var(--border)] text-[var(--foreground)] focus:outline-none focus:border-[var(--foreground)] transition-colors"
              placeholder="Your name"
            />
          </div>

          <div>
            <label for="email" class="block text-sm text-[var(--secondary-foreground)] mb-2">
              Email
            </label>
            <input
              type="email"
              id="email"
              name="email"
              required
              class="w-full px-4 py-3 bg-[var(--background)] border-2 border-[var(--border)] text-[var(--foreground)] focus:outline-none focus:border-[var(--foreground)] transition-colors"
              placeholder="your.email@example.com"
            />
          </div>

          <div>
            <label for="message" class="block text-sm text-[var(--secondary-foreground)] mb-2">
              Message
            </label>
            <textarea
              id="message"
              name="message"
              required"
              rows="5"
              class="w-full px-4 py-3 bg-[var(--background)] border-2 border-[var(--border)] text-[var(--foreground)] focus:outline-none focus:border-[var(--foreground)] transition-colors resize-none"
              placeholder="Your message..."
            ></textarea>
          </div>

          <button
            type="submit"
            class="w-full px-8 py-3 bg-[var(--foreground)] text-[var(--background)] font-medium hover:bg-[var(--accent)] hover:scale-105 transition-all duration-300"
          >
            Send Message
          </button>
        </form>
      </div>
    </div>
  </div>
</section>

<style>
  .scroll-reveal {
    opacity: 0;
    animation: fadeInUp 0.8s ease-out forwards;
    animation-timeline: view();
    animation-range: entry 0% cover 30%;
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  input::placeholder,
  textarea::placeholder {
    color: var(--muted-foreground);
    opacity: 0.6;
  }
</style>
